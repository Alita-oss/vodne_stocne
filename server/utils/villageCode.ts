import { VillageName } from '../types/village';

export const VillageCodeMap: Record<VillageName, number> = {
    [VillageName.LIPA]: 1,
    [VillageName.PETRKOV]: 2,
    [VillageName.DOBROHOSTOV]: 3,
};
